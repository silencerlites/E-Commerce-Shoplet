# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @shoplet/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t shoplet-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system shoplet-auth-service && \
          adduser --system -G shoplet-auth-service shoplet-auth-service

COPY apps/auth-service/dist shoplet-auth-service/
COPY apps/auth-service/package.json shoplet-auth-service/
RUN chown -R shoplet-auth-service:shoplet-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix shoplet-auth-service --omit=dev -f install

CMD [ "node", "shoplet-auth-service" ]
